version: 2.2
orbs: # adds orbs to your configuration
  jira: circleci/jira@1.3.1 # invokes the Jira orb, making its commands accessible

jobs: 
  build_and_test:
    docker:
    - image: cimg/node:13.11.0
    steps:
    - add_ssh_keys:
        fingerprints: 
        - "a5:4b:c1:6b:25:88:e8:b8:98:60:a5:cb:99:41:00:b1" #additional key
        # - "8b:af:af:73:21:2e:2a:7f:b3:73:74:a9:5f:c0:f6:41" #deployment key
    - run:
        name: SSH Disable Host Key Checking
        command: |
          echo "StrictHostKeyChecking=no">> ~/.ssh/config        
    - checkout
    - run:
        command: |
          chmod 755 .circleci/deploy.sh
          if [ ! -f "package.json" ]; then
            echo
            echo "---"
            echo "Unable to find your package.json file. Did you forget to set the app-dir parameter?"
            echo "---"
            echo
            echo "Current directory: $(pwd)"
            echo
            echo
            echo "List directory: "
            echo
            ls
            exit 1
          fi
        name: Checking for package.json.
        working_directory: ~/project
    - restore_cache:
        keys:
        - node-deps-v1-{{ .Branch }}-{{ checksum "~/project/package-lock.json" }}
    - run:
        command: "if [[ ! -z \"\" ]]; then\n  echo \"Running override package installation command:\"\n  \nelse\n  npm ci\nfi\n"
        name: Installing NPM packages
        working_directory: ~/project
    - save_cache:
        key: node-deps-v1-{{ .Branch }}-{{ checksum "~/project/package-lock.json" }}
        paths:
        - ~/.npm
    - run:
        command: npm run test
        name: Run NPM Tests
        working_directory: ~/project
    - run:
        name: DEV
        command: export NODE_ENV=development
    - run:
        name: Installing NPM packages
        command: "if [[ ! -z \"\" ]]; then\n  echo \"Running override package installation command:\"\n  \nelse\n  npm ci\nfi\n"
        working_directory: ~/project
    # - run:
    #     name: Deploy
    #     command: ./.circleci/deploy.sh
  
  # deploy-job:
  # ssh -o "StrictHostKeyChecking no" ${SSH_USER}@${SSH_HOST} \<<- EOF
  # ssh -o "StrictHostKeyChecking no" "ubuntu@ec2-3-229-45-120.compute-1.amazonaws.com" cat \<<EOF
    # - run:
    #     name: Deploy
    #     command: |
    #       echo "============================================ Deploying ============================================"
    #       ssh -o "StrictHostKeyChecking no" "ubuntu@ec2-3-229-45-120.compute-1.amazonaws.com"
    #         echo "Building..."
    #         git pull
    #       EOF
    # - run:
    #     name: DeployGitPull
    #     command: |
    #       cd circleci-test && git branch development && git pull
    - run:
        name: Deploy
        command: |-
          ssh $SSH_USER@$SSH_HOST 'bash -c CURRDATE=$(date +%s);echo \$CURRDATE; cd circleci-test/ && \
            git branch development && \
            git pull && \"'

workflows:
  version: 2
  node-tests:
    jobs:
      - build_and_test:
          context: development

      - deploy-job:
          requires:
            - build_and_test
          filters:
            branches:
              only: master        
  

# Original config.yml file:
# version: 2.1
# orbs:
#   node: circleci/node@3.0.0
# workflows:
#   node-tests:
#     jobs:
#       - node/test